# AWS Configuration
aws_region  = "ap-southeast-2" # Change to your region
environment = "test"           # or "prod"

# CloudWatch Configuration
log_retention_days = 7 # 7 for test, 30 for prod

# Lambda Configuration
lambda_memory_size = 128 # 128 for test, 256+ for prod
lambda_timeout     = 30

# Plugin version
plugin_version = "1.0.0"

# Note: jmap-service-core integration values (table name, API URL, etc.)
# are automatically discovered via SSM Parameter Store.
# Ensure jmap-service-core is deployed first.
